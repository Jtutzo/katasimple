spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:~/katasimple-test;DB_CLOSE_DELAY=-1
    username: katasimple
    password: katasimple
axon:
  eventhandling:
    processors:
      user:
        mode: subscribing
